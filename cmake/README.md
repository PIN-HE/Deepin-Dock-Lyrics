# CMake helpers

This directory is reserved for project-local CMake helpers. S00 does not need a custom helper yet; keeping the directory in the source tree makes the planned package and translation integration location explicit.
